
/*
import report from '@/services/reportHandler'

// Mocs
import httpService from '@/services/httpService'

/*
jest.mock('@/services/httpService', () => ({
  post: jest.fn().mockResolvedValue({
    data: 'response data'
  }),
  get: jest.fn().mockResolvedValue({
    data: 'response data'
  })
}))

test('Report exists', () => {
  expect(report).toBeTruthy()
})

test('Get reports returns content', async () => {
  await expect(report.getAvailableReports())
    .resolves.toBeTruthy()

  await expect(report.getAvailableReports())
    .resolves.not.toHaveLength(0)

  expect(httpService.get).toHaveBeenCalled()
})

test('Get intervals returns content', async () => {
  await expect(report.getSuggestedIntervals())
    .resolves.toBeTruthy()

  await expect(report.getSuggestedIntervals())
    .resolves.not.toHaveLength(0)
})

*/
/*
test('Get default data', async () => {
  let data

  await report.initialize().then(defaultData => { data = defaultData })

  expect(data).toHaveProperty('availableReports')
  expect(data).toHaveProperty('suggestedIntervals')
  expect(data).toHaveProperty('reportData')
})

*/
/*
test('Report exists', () => {
  expect(httpService.setAddress).toHaveBeenCalled()
})sa
*/
